---
name: terraform

"on":
  push:
    branches:
      - "main"
    paths:
      - "terraform/**"
  pull_request:
    paths:
      - "terraform/**"

jobs:
  lint:
    runs-on: ubuntu-26.04
    steps:
      - uses: actions/checkout@v6
      - run: make tflint
